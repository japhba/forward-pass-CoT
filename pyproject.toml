[project]
name = "forward-pass-cot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.9",
    "transformers>=4.57",
    "scikit-learn>=1.8",
    "matplotlib>=3.10",
    "tqdm>=4.67",
    "numpy>=2.2",
]
